<h1>Listing bikes</h1>

<table>
  <thead>
    <tr>
      <th>Bike</th>
      <th>Checked Out?</th>
      <th>Flagged for Repair?</th>
      <th>User Comments before inspection</th>
      <th>Addtitional Repair Needed?</th>
      <th>Last Date Inspected</th>
    </tr>
  </thead>

  <tbody>
    <% @bikes.order(:bike_id).each do |bike| %>
      <tr>
        <td><%= bike.bike_id %></td>
        <td><%= bike.checked_out %></td>
        <td><%= bike.need_repair %></td>
        <td><%= bike.problem_description %></td>
        <td><%= bike.addtional_repair_need %></td>
        <td><%= bike.last_date_inspected %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

