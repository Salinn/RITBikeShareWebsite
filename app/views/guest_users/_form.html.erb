<%= form_for(@guest_user) do |f| %>
  <% if @guest_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guest_user.errors.count, "error") %> prohibited this guest_user from being saved:</h2>

      <ul>
      <% @guest_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rit_user_id %><br>
    <%= f.number_field :rit_user_id %>
  </div>
  <div class="field">
    <%= f.label :other_email %><br>
    <%= f.text_field :other_email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
