<%= form_for(@rit_user) do |f| %>
  <% if @rit_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rit_user.errors.count, "error") %> prohibited this rit_user from being saved:</h2>

      <ul>
      <% @rit_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :rit_email %><br>
    <%= f.text_field :rit_email %>
  </div>
  <div class="field">
    <%= f.label :admin %><br>
    <%= f.check_box :admin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
