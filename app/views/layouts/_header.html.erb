<header class="navbar navbar-fixed-top navbar-inverse nav-collapse collapse">
  <div class="navbar-inner ">
    <div class="container nav-collapse collapse">
      <% if user_signed_in? && (current_user.admin || current_user.checkout_person ) %>
          <%= link_to image_tag("bike_logo_web.png",:size => "60"), home_check_out_path, id: "logo" %>
      <% elsif user_signed_in? %>
          <%= link_to image_tag("bike_logo_web.png",:size => "60"), user_home_path, id: "logo" %>
      <% else %>
          <%= link_to image_tag("bike_logo_web.png",:size => "60"), root_path, id: "logo" %>
      <% end %>
      <nav>
        <ul class="nav pull-right">
          <% if user_signed_in? && (current_user.admin || current_user.checkout_person )%>
              <li><%= link_to "Home",    home_check_out_path %></li>
          <% elsif user_signed_in? %>
              <li><%= link_to "Home",    user_home_path %></li>
          <% else %>
              <li><%= link_to "Home",    root_path %></li>
          <% end %>
          <li><%= link_to "FAQ",    help_path %></li>
          <% if user_signed_in? %>
              <% if current_user.registered %>
                  <li><%=  link_to "Account", current_user %></li>
              <% else %>
                  <li><%=  link_to "Register", register_path %></li>
              <% end %>
              <% if current_user.repairman %>
                  <li><%= link_to "Reports", reports_path %></li>
                  <li><%= link_to "View Reports", bikes_path %></li>
              <% end %>
              <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
          <% else %>
              <li><%= link_to "Sign in", new_user_session_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
